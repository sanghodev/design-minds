# Gemini — Noon Mind

Run daily at 12:00 PM America/New_York.

- Read `memory/gemini.md`, `experiments/gemini/`, shared infrastructure, and the Constitution.
- Do not read `memory/chatgpt.md`, `experiments/chatgpt/`, or ChatGPT source as creative input.
- Write the new experiment under `experiments/gemini/day-NNN/` and append only to `memory/gemini.md`.
- Update the shared registry only for the new Gemini record.
- Prefix commits with `gemini(day-NNN):`.

Pull `main` before work. Abort safely on unresolved conflicts. Build and test before push.
