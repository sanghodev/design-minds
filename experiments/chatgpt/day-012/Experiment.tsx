"use client";
import { useState } from "react";
import Link from "next/link";
import s from "./study.module.css";
function Nav({day}:{day:number}){return <nav className={s.nav}><Link href="/">Design Minds</Link><span>CHATGPT / DAY {String(day).padStart(3,'0')}</span></nav>}
function Foot({day}:{day:number}){return <footer><Link href={`/research/chatgpt/day-${String(day).padStart(3,'0')}`}>Research notebook (Korean)</Link><p>Independent design study · local interactions only · no participant data collected.</p></footer>}
export default function Experiment(){const [gap,setGap]=useState(25);const [reference,setReference]=useState(false);const [judgment,setJudgment]=useState('Not recorded');const circumference=2*Math.PI*130;return <main className={s.page} lang="en"><Nav day={12}/><h1>The edge<br/>you complete.</h1><p>Look before revealing the reference. Is this one loop, separate pieces, or uncertain?</p><figure className={s.stage}><svg viewBox="0 0 600 340" role="img" aria-label={`Four circular arcs, each with a ${gap} degree gap. Reference ${reference?'visible':'hidden'}.`}>{reference&&<circle cx="300" cy="170" r="130" fill="none" stroke="#929990" strokeWidth="1"/>}<circle cx="300" cy="170" r="130" fill="none" stroke="currentColor" strokeWidth="7" strokeDasharray={`${circumference*(90-gap)/360} ${circumference*gap/360}`} transform="rotate(-90 300 170)"/></svg><figcaption>Four fixed arcs. Only the missing angle changes.</figcaption></figure><label>Gap per quarter: {gap}°<input type="range" min="5" max="80" value={gap} onChange={e=>{setGap(+e.target.value);setJudgment('Not recorded')}}/></label><div className={s.controls}>{['One loop','Separate pieces','Uncertain'].map(v=><button key={v} aria-pressed={judgment===v} onClick={()=>setJudgment(v)}>{v}</button>)}</div><p role="status">Your current judgment: {judgment}. Stored only in this page.</p><div className={s.controls}><button aria-pressed={reference} onClick={()=>setReference(!reference)}>Reveal reference</button><button onClick={()=>{setGap(25);setReference(false);setJudgment('Not recorded')}}>Reset</button></div><p className={s.note}>Reset restores the drawing, not what you have learned. This is not a measured perceptual threshold.</p><Foot day={12}/></main>}
