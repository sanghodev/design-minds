"use client";
import { useState } from "react";
import Link from "next/link";
import s from "./study.module.css";
function Nav({day}:{day:number}){return <nav className={s.nav}><Link href="/">Design Minds</Link><span>CHATGPT / DAY {String(day).padStart(3,'0')}</span></nav>}
function Foot({day}:{day:number}){return <footer><Link href={`/research/chatgpt/day-${String(day).padStart(3,'0')}`}>Research notebook (Korean)</Link><p>Independent design study · local interactions only · no participant data collected.</p></footer>}
export default function Experiment(){const [rule,setRule]=useState('Area');const [show,setShow]=useState(false);const values=[1,4,9];return <main className={s.page} lang="en"><Nav day={18}/><p>QUANTITY / GEOMETRY</p><h1>The weight<br/>of a circle.</h1><p>Same values. Same largest circle. Change what the number controls.</p><div className={s.controls}>{['Area','Radius'].map(v=><button key={v} aria-pressed={rule===v} onClick={()=>setRule(v)}>{v} proportional to value</button>)}</div><figure className={s.stage}><svg viewBox="0 0 600 240" role="img" aria-label={`Values 1, 4, 9 mapped proportionally to ${rule.toLowerCase()}. Largest radius is 80 drawing units.`}>{values.map((v,i)=><g key={v}><circle cx={100+i*200} cy="110" r={80*(rule==='Area'?Math.sqrt(v/9):v/9)} fill="#185544"/><text x={100+i*200} y="220" textAnchor="middle" fill="currentColor" fontSize="24">{v}</text></g>)}</svg><figcaption>Authored values. Geometry is computed; reader interpretation is not measured.</figcaption></figure><div className={s.controls}><button aria-pressed={show} onClick={()=>setShow(!show)}>Show calculation</button><button onClick={()=>{setRule('Area');setShow(false)}}>Reset</button></div>{show&&<div className={s.stage}><p>Area = π × radius². {rule==='Area'?'Radius follows the square root of value.':'Radius follows value, so area follows value squared.'}</p><table><caption>Ratios relative to the smallest circle</caption><thead><tr><th>Value</th><th>Radius ratio</th><th>Area ratio</th></tr></thead><tbody>{values.map(v=><tr key={v}><td>{v}</td><td>{rule==='Area'?Math.sqrt(v):v}</td><td>{rule==='Area'?v:v*v}</td></tr>)}</tbody></table></div>}<p>Accurate area encoding does not guarantee accurate area judgments. Compare your estimate before and after opening the calculation.</p><Foot day={18}/></main>}
