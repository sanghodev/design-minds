"use client";
import { useState } from "react";
import Link from "next/link";
import s from "./study.module.css";
function Nav({day}:{day:number}){return <nav className={s.nav}><Link href="/">Design Minds</Link><span>CHATGPT / DAY {String(day).padStart(3,'0')}</span></nav>}
function Foot({day}:{day:number}){return <footer><Link href={`/research/chatgpt/day-${String(day).padStart(3,'0')}`}>Research notebook (Korean)</Link><p>Independent design study · local interactions only · no participant data collected.</p></footer>}
export default function Experiment(){const [size,setSize]=useState(24);const [reveal,setReveal]=useState(false);const [selected,setSelected]=useState('None');return <main className={s.page} lang="en"><Nav day={16}/><h1>The shape<br/>of a hit.</h1><p>Three fixed marks. Try selecting near a mark, then reveal the boundary that accepted your input.</p><div className={s.controls}>{[24,44,64].map(v=><button key={v} aria-pressed={size===v} onClick={()=>setSize(v)}>{v} px target</button>)}<button aria-pressed={reveal} onClick={()=>setReveal(!reveal)}>Reveal hit areas</button></div><section className={s.stage} aria-label="Target field" style={{display:'flex',justifyContent:'space-around',alignItems:'center',minHeight:300}}>{['A','B','C'].map(id=><div key={id} style={{width:72,textAlign:'center'}}><button aria-label={`Select target ${id}`} onClick={()=>setSelected(id)} style={{width:size,height:size,minHeight:size,padding:0,border:reveal?'1px dashed currentColor':'1px solid transparent',display:'inline-flex',alignItems:'center',justifyContent:'center',background:selected===id?'#d6e4da':'transparent'}}><span aria-hidden="true" style={{fontSize:18,lineHeight:'18px'}}>+</span></button><p>{id}</p></div>)}</section><p role="status">Selected target: {selected}</p><details><summary>Alternative selection controls</summary><div className={s.controls}>{['A','B','C'].map(id=><button key={id} onClick={()=>setSelected(id)}>Select {id}</button>)}</div><p>These larger controls provide access to the same selection. They do not reproduce the pointing task.</p></details><button onClick={()=>{setSize(24);setReveal(false);setSelected('None')}}>Reset</button><p className={s.note}>Target dimensions are CSS pixels. No speed, miss count or user performance is measured.</p><Foot day={16}/></main>}
