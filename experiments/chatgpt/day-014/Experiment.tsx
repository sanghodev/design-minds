"use client";
import { useState } from "react";
import Link from "next/link";
import s from "./study.module.css";
function Nav({day}:{day:number}){return <nav className={s.nav}><Link href="/">Design Minds</Link><span>CHATGPT / DAY {String(day).padStart(3,'0')}</span></nav>}
function Foot({day}:{day:number}){return <footer><Link href={`/research/chatgpt/day-${String(day).padStart(3,'0')}`}>Research notebook (Korean)</Link><p>Independent design study · local interactions only · no participant data collected.</p></footer>}
const point=(t:number)=>({x:40+520*t,y:160-90*Math.sin(t*Math.PI*4)});
export default function Experiment(){const [samples,setSamples]=useState(5);const [phase,setPhase]=useState(0);const [path,setPath]=useState(false);const current=point(phase/100);const line=Array.from({length:101},(_,i)=>{const p=point(i/100);return `${i?'L':'M'}${p.x},${p.y}`}).join(' ');return <main className={s.page} lang="en"><Nav day={14}/><p>SAMPLING / AN AUTHORED TRAJECTORY</p><h1>Between<br/>the frames.</h1><p>Move through time yourself. What can the visible samples tell you about the missing interval?</p><figure className={s.stage}><svg viewBox="0 0 600 320" role="img" aria-label={`${samples} samples on an authored wave trajectory. Current time ${phase} percent. Full path ${path?'shown':'hidden'}.`}>{path&&<path d={line} fill="none" stroke="#647569" strokeWidth="2"/>}{Array.from({length:samples},(_,i)=>{const p=point(i/(samples-1));return <circle key={i} cx={p.x} cy={p.y} r="7" fill="none" stroke="currentColor" strokeWidth="2"/>})}<circle cx={current.x} cy={current.y} r="12" fill="#ad371d"/></svg><figcaption>Outlined circles: evenly spaced time samples. Solid dot: manually selected time.</figcaption></figure><label>Samples: {samples}<input type="range" min="3" max="25" step="2" value={samples} onChange={e=>setSamples(+e.target.value)}/></label><label>Time: {phase}%<input type="range" min="0" max="100" value={phase} onChange={e=>setPhase(+e.target.value)}/></label><div className={s.controls}><button aria-pressed={path} onClick={()=>setPath(!path)}>Reveal authored path</button><button onClick={()=>{setSamples(5);setPhase(0);setPath(false)}}>Reset</button></div><p>No autoplay. This is a computed path, not recorded motion or a video frame-rate test.</p><Foot day={14}/></main>}
