"use client";
import { useState } from "react";
import Link from "next/link";
import s from "./study.module.css";
function Nav({day}:{day:number}){return <nav className={s.nav}><Link href="/">Design Minds</Link><span>CHATGPT / DAY {String(day).padStart(3,'0')}</span></nav>}
function Foot({day}:{day:number}){return <footer><Link href={`/research/chatgpt/day-${String(day).padStart(3,'0')}`}>Research notebook (Korean)</Link><p>Independent design study · local interactions only · no participant data collected.</p></footer>}
export default function Experiment(){const [pitch,setPitch]=useState(16);const [coverage,setCoverage]=useState(25);const radius=pitch*Math.sqrt(coverage/100/Math.PI);const gray=Math.round(255*(1-coverage/100));return <main className={s.page} lang="en"><Nav day={15}/><h1>A tone<br/>made of dots.</h1><p>Keep ink coverage fixed while changing the screen pitch. Does the texture become a tone?</p><div className={s.columns}><figure className={s.stage}><svg viewBox="0 0 320 320" role="img" aria-label={`Black dots with ${coverage} percent nominal area coverage and ${pitch} unit pitch.`}><defs><pattern id="day15-screen" width={pitch} height={pitch} patternUnits="userSpaceOnUse"><rect width={pitch} height={pitch} fill="white"/><circle cx={pitch/2} cy={pitch/2} r={radius} fill="black"/></pattern></defs><rect width="320" height="320" fill="url(#day15-screen)"/></svg><figcaption>Periodic dots · nominal area {coverage}%</figcaption></figure><figure className={s.stage}><div role="img" aria-label={`Uniform sRGB gray ${gray} of 255, not perceptually calibrated.`} style={{aspectRatio:'1',background:`rgb(${gray},${gray},${gray})`}}/><figcaption>Arithmetic sRGB reference · {gray}/255</figcaption></figure></div><label>Screen pitch: {pitch} drawing units<input type="range" min="4" max="40" step="4" value={pitch} onChange={e=>setPitch(+e.target.value)}/></label><label>Separate intervention — nominal coverage: {coverage}%<input type="range" min="10" max="60" step="5" value={coverage} onChange={e=>setCoverage(+e.target.value)}/></label><button onClick={()=>{setPitch(16);setCoverage(25)}}>Reset</button><p>Radius = pitch × √(coverage / π). The reference is not luminance-matched. Edge clipping, antialiasing, zoom and viewing distance affect the displayed result.</p><Foot day={15}/></main>}
