"use client";
import { useState, useRef } from "react";
import Link from "next/link";
import s from "./study.module.css";
function Nav({day}:{day:number}){return <nav className={s.nav}><Link href="/">Design Minds</Link><span>CHATGPT / DAY {String(day).padStart(3,'0')}</span></nav>}
function Foot({day}:{day:number}){return <footer><Link href={`/research/chatgpt/day-${String(day).padStart(3,'0')}`}>Research notebook (Korean)</Link><p>Independent design study · local interactions only · no participant data collected.</p></footer>}
const records=[{id:'clay',title:'Clay',year:2019},{id:'paper',title:'Paper',year:2024},{id:'brass',title:'Brass',year:2021},{id:'linen',title:'Linen',year:2017},{id:'glass',title:'Glass',year:2023}];
export default function Experiment(){const [sort,setSort]=useState('Original');const [selected,setSelected]=useState('brass');const refs=useRef<Record<string,HTMLButtonElement|null>>({});const ordered=[...records].sort((a,b)=>sort==='Title'?a.title.localeCompare(b.title):sort==='Year'?a.year-b.year:0);return <main className={s.page} lang="en"><Nav day={17}/><p>IDENTITY / POSITION</p><h1>A place<br/>to return.</h1><p>Select a record, then reorder the collection. What should follow the record?</p><div className={s.controls}><label>Sort order<select value={sort} onChange={e=>setSort(e.target.value)}>{['Original','Title','Year'].map(v=><option key={v}>{v}</option>)}</select></label><button onClick={()=>refs.current[selected]?.focus()}>Return to selected</button><button onClick={()=>{setSort('Original');setSelected('brass')}}>Reset</button></div><p role="status">Selected: {records.find(x=>x.id===selected)?.title}. Position {ordered.findIndex(x=>x.id===selected)+1} of {records.length}.</p><ol className={s.stage} style={{paddingLeft:32}}>{ordered.map(x=><li key={x.id} style={{padding:'12px 0',borderBottom:'1px solid #aaa'}}><button ref={el=>{refs.current[x.id]=el}} aria-pressed={selected===x.id} onClick={()=>setSelected(x.id)} style={{width:'100%',textAlign:'left',display:'flex',justifyContent:'space-between'}}><span>{x.title}{selected===x.id?' — selected':''}</span><span>{x.year}</span></button></li>)}</ol><p>Authored records, not collection metadata. Sorting changes both visual and reading order. Focus moves to the selected record only when you request it.</p><Foot day={17}/></main>}
